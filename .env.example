# Port for server.js (after npm run build)
PORT=3000

# Optional: path to built site if not ./dist (relative to server.js)
# STATIC_DIR=dist

# Optional HTTPS — leave unset for HTTP only (typical behind nginx/Caddy)
# SSL_KEY_PATH=/path/to/privkey.pem
# SSL_CERT_PATH=/path/to/fullchain.pem
